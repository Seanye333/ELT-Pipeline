# IMPORTANT: Populate these values from your secrets manager (Vault, Jenkins credentials, etc.)
# Never commit real secrets — use sealed-secrets or external-secrets-operator in production.
apiVersion: v1
kind: Secret
metadata:
  name: graph-api-secret
  namespace: elt-pipeline
type: Opaque
stringData:
  GRAPH_TENANT_ID: "REPLACE_ME"
  GRAPH_CLIENT_ID: "REPLACE_ME"
  GRAPH_CLIENT_SECRET: "REPLACE_ME"
  GRAPH_ONEDRIVE_USER_ID: "REPLACE_ME"
  ONEDRIVE_FOLDER_PATH: "/Reports/Monthly"
